# ArtaleHUD
A Python desktop app with a transparent overlay showing multiple circular timers. Each timer supports start, pause, resume, stop, and auto-reset with hotkey control. Timers are configurable via a control panel with names, durations, and hotkeys. Built with PyQt6 and pynput, it updates smoothly and optionally beeps when finished.
